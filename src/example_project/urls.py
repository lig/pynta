from pynta import urls

urls += (
    (r'^/$', 'index', 'index'),
)
urls += (
    (r'^other/$', 'other', 'other'),
)
urls += (
    (r'^other1/$', 'other1', 'other1'),
    (r'^other2/$', 'other2', 'other2'),
)
